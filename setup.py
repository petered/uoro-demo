from setuptools import setup, find_packages

setup(
    name = "uoro-demo",
    author = "peter",
    version = 0,
    dependency_links = (),
    install_requires = ['numpy', 'matplotlib', 'pytorch'],
    scripts = [],
    packages=find_packages(),
    )
